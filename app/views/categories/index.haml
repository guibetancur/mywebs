%h1 Categorías
%table
   %tr
      %th Nombre
      %th Descripcion
   - @categories.each do |category|
      %tr
         %td= category.name
         %td= category.description
         %td= link_to 'Subcategorías', category_subcategories_path(category)
= link_to 'Nueva categoría', new_category_path
